# Design: add-report-side-panel

## 决策

1. **渲染逻辑抽离复用**：报告渲染抽为独立组件，消息流摘要卡与面板共用数据、面板独占渲染，避免两份渲染实现漂移。
2. **面板不替代消息内摘要**：消息流保留摘要卡片作为时间线锚点，面板承担深读——符合「对话是过程、文档是产物」的形态。
3. **移动端回退**：窄屏下面板体验差，移动端报告维持消息流内全宽展示。

## 风险

- **与居中窄栏叠加**：replicate-chat-home 的 max-w-3xl 与右侧面板并存时，内容区需为面板让出宽度——布局验收覆盖两 change 组合。
