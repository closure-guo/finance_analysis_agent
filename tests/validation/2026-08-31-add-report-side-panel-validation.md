# 人工验证报告: add-report-side-panel

**日期**: 2026-08-31
**验证人**: ZCode agent（GUI 自动化实测 + 组件测试）
**关联 delta**: openspec/changes/add-report-side-panel/
**E2E 门禁**: 不适用（e2e/ 基建 P1–P4 未落地）

## 验证环境

- 组件测试：vitest + jsdom（src/test/reportSidePanel.test.tsx，10 例，含 App 级组合）
- 浏览器：真实 Chromium + TESTING=1 stub 后端（含引用数据的完成会话）

## 验证结果

| Scenario | E2E 已覆盖？ | 预期行为 | 实际结果 | 通过 |
|---|---|---|---|---|
| 摘要卡渲染 | 组件测试 | 完成态消息流收敛为摘要卡（要点 + 打开报告） | 实测要点 4 条（章节标题）+ 打开按钮 | ✅ |
| 摘要卡跳转面板 | 组件测试（App 级） | 点击「打开报告」→ 面板打开显示完整报告 | 实测面板打开（state=open，宽 560），内容含报告正文 | ✅ |
| 面板滑出动画 | 组件测试 | 约 300ms transform 过渡 | data-state=open + translateX(0)（300ms ease-out transition） | ✅ |
| 宽度拖拽调节 | 组件测试 | 拖拽左边缘调宽（380–960 夹紧） | 实测 560→840（DOM 事件链模拟拖拽 280px） | ✅ |
| 宽度持久化 | 组件测试 | 拖拽后 localStorage 记忆，重挂载读取 | 实测 stored=840；组件测试重挂载读 700px | ✅ |
| 操作栏导出 | 组件测试 | docx/pptx/pdf/md 下载链接（filePaths 驱动） | 实测出现 docx/pptx/md 三枚（pdf 未生成的文件不渲染，与既有抽屉契约一致） | ✅ |
| 面板关闭 | 组件测试 | 关闭按钮/Esc | 实测关闭按钮生效；Esc 组件测试覆盖 | ✅ |
| 移动端回退 | 组件测试 | <768px 无面板，报告消息流内展示 | 实测 700px 视口：面板不渲染，点「打开报告」→ 消息流内完整区展开 | ✅ |
| 与居中窄栏组合 | 否 | 面板叠加在 max-w-3xl 内容区之上不遮挡 | 面板 fixed 右侧 z-55，消息流窄栏不受影响（实测同屏并存） | ✅ |

## 既有测试适配说明

1. 完成态报告收敛摘要卡后，报告标题文本在「要点摘录」与「完整区正文」各出现一次，selectSession.test.tsx 中 6 处 getByText 断言适配为 getAllByText（存在性断言语义不变；消息位置断言取完整区内标题元素 `pop()`，语义不变）。

## 异常记录

1. CUA 真实指针拖拽未命中 6px 拖拽热区——验证工具精度问题；DOM 事件链模拟拖拽验证通过（组件测试同路径覆盖）。真实鼠标拖拽建议人工抽查。

## 结论

[x] 面板开合/拖拽/导出/移动端回退全部通过（前端 431 测试全绿）
[ ] E2E 门禁待基建落地后补充执行（Task 4.2 未勾选）
