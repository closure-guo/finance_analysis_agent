# P1 注入法反幻觉消融 pilot 校准报告（首次启动）

日期：2026-09-16
delta：`revamp-ablation-v2-causal-claims`（worktree `.worktrees/ablation-v2`）
预登记：`evals/ablation/preregister/2026-09-16-p1-injection-pilot.md`（门禁通过，valid=True）
执行环境：本机 + 火山方舟（glm-5.3）；Langfuse 在线；10 标的基准材料为 analysts 变体真跑

## 0. 执行摘要

| 项 | 结果 |
|---|---|
| 单元 | 213 = **ok 202 / void 10 / error 1** |
| 离线腿 | **200 单元，0 LLM 调用（已核验）** |
| 真跑腿（限量 12） | 13 单元记录 / **204 LLM 调用**（修正后账目，见 §4） |
| 阳性对照 | **显著**（n=82，b=57，c=1，exact p=4.09e-16）→ 管线灵敏度证实，本轮阴性结论不作废 |
| 停止规则 | 总体不一致比 **0.485** 落健康区间 → 可进入人工终裁；终裁 **201 条 < 300 上限** |
| 逃逸率 | `rate=None`（201 条 pending，未终裁不报数——口径纪律） |

## 1. 分型校准（离线腿，n=40/型，零 LLM）

| 污染型 | 机制 | n | b | c | 不一致比 | 校准 | 读法 |
|---|---|---|---|---|---|---|---|
| direction_error | A3 | 40 | 20 | 0 | **0.500** | **ok** | A3 拦截约一半方向错误；另一半未到达/未触发，强度可维持 |
| unit_error | A3 | 40 | 36 | 0 | **0.900** | **ok**（贴 too_hard 边界） | 单位归一化拦住绝大多数；再强一点就只剩该机制独扛 |
| period_shift | A2 | 40 | 40 | 0 | **1.000** | **too_hard** | 仅期次检查能拦——其余层零冗余；按停止规则**降强度/换注入点**，不硬扩样本 |
| fabricated_event | A6 | 40 | 0 | 0 | 0.000 | too_easy | **结构性盲区**：A6 无 FAIL 面（只产 PASS/UNVERIFIABLE） |
| stale_macro | A7 | 40 | 0 | 0 | 0.000 | too_easy | **结构性盲区**：A7 的新鲜度标记只进 context，不进校验链 |
| 合计 | — | 202 | 97 | 1 | **0.485** | **ok** | — |

**盲区（如实登记，拦截率 0 不可读作机制无价值）**：A6/A7 在校验链上无 FAIL 面——须按成本分型改读输入侧/决策层证据，或在后续批次为其定义可测面（登记候选）。

## 2. 真跑腿（限量 12，逐条如实记录）

| 型 | 单元 | 结果 |
|---|---|---|
| value_error（context→income_statement） | 5 | **2 ok**：`600519-value_error-0` 开态=escaped/关态=caught（**反向 c**，单例）；`000001-value_error-0` 开态=caught/关态=escaped（机制方向正确）。n=2 → **insufficient** |
| value_error / mirror_narrative | 3 + 4 | **7/8 void**：「两态终态产物均无污染值且无告警（污染未到达产物）」——**context 级注入未传导到终态**：营收格 ±5–50% 与 K 线反转均未被分析师写进产物。→ **真跑型注入强度/靶点需重校**（停止规则精神：不硬扩样本） |
| illegal_price（decision） | 4 | 3 void + 1 error：产物快照缺 `trader_plan`/`final_trade_decision`（材料步只跑 analysts 变体）→ **决策层注入钩子待建**；错误单元见 §3 |

## 3. P1 命中的真实缺陷（均已修复）

1. **`anchors` 字符串形态致 DebateMessage 校验崩溃**（`600519-illegal_price-2`，全图运行中）：LLM 把 `anchors` 返回为字符串 → pydantic 抛错 → 图运行失败。这是 adba 收紧契约后引入的崩溃路径，**野外首次实证**。修复：`09a5029`（形态归一：str→单元素列表、不可解释→[]，text 空仍拒），32 passed。
2. **预算漏计**：真跑腿实际 204 次 LLM 调用，报告仅计 20（只算 ok 单元）——违反「预算分型申报」的诚实要求。修复：`fdfeb9f`（全量计账 + 按状态拆项 + 计量器交叉核对）。
3. **void 单元先跑后废**：illegal_price 4 单元在 `applied=False` 已知的情况下仍各烧 38 次调用（114 次）才判 void。修复：`fdfeb9f`（不可施加注入**预判于图运行之前**，0 次图运行/0 次 LLM；按现材料，全部 10 标的的 illegal_price 每轮可省 ~1520 次调用）。

## 4. 预算（修正后，分型申报）

| 腿 | 单元 | LLM 调用 |
|---|---|---|
| offline_replay | 200 | **0**（`zero_llm_verified=true`） |
| real_run | 13（ok 2 / void 10 / error 1） | **204**（含 void 114、error 38；`llm_meter_matches_unit_sum` 可核） |
| 另计：基准材料生成 | 10 标的 × analysts 变体 | 未并入跑批账（材料步独立，产物路径见 `reports/ablation/p1/materials/`） |

两类成本量级不同，SHALL NOT 合并为单一 run 等效数（spec 注入成本结构二分）。

## 5. 终裁工作清单

- **201 条待人工终裁** → `tests/validation/2026-09-16-p1-adjudication-worklist.csv`（列：unit_id / 型 / 机制 / 腿 / 开态 / 关态 / 污染值 / 证据路径 / 人工裁定留空列）
- 终裁口径（spec）：**污染值出现在终态产物且未被拦截 = 真逃逸**；逐条确认后回填，`escape_rate` 才会从 `rate=None` 出数
- 工作量：201 < 预登记上限 300 ✓

## 6. 结论与下一步

**达标**：direction_error（0.50）、unit_error（0.90）落健康区间；阳性对照显著；离线腿零 LLM 通路成立（spec「离线重放不消耗 LLM」实证）。
**待重校（pilot 目的本身）**：period_shift（too_hard，降强度/换点）；context 级真跑注入（7/8 未到达产物，需强化靶点——建议改为「注入到分析师必然引用的字段」或经 claim 级注入）。
**结构性缺口（登记）**：A6/A7 校验面无 FAIL 面；illegal_price 决策层钩子（材料步需捕获 decision dict）；`anchors` 形态归一入 spec（候选 delta）。
**本轮不作为正式结论**：pilot 为强度校准轮；逃逸率待终裁。

---

## 7. Round 2（重校轮，2026-09-16，commit `63e6347`）

**重校项与效果**（按预登记停止规则的逐型判定执行）：

| 型 | round 1 | round 2 | 变化说明 |
|---|---|---|---|
| period_shift | 1.000 too_hard | **0.500 ok** | 双形态实例（偶=仅标签；奇=标签+field_ref 段位移）→ 一半实例被值检查在**双态**拦下 |
| direction_error | 0.500 ok | 0.500 ok | 不变 |
| unit_error | 0.900 ok | 0.900 ok | 不变 |
| fabricated_event / stale_macro | 0.000 | 0.000 | 盲区不变（A6/A7 无 FAIL 面，结构性） |
| **合计** | 0.485 ok | **0.374 ok** | 仍在健康区间 |

**真跑腿 round 2**（12 单元 / **157 LLM 调用**，meter 交叉核对一致）：

- `value_error` ×4 仍 void：「污染未到达产物」——分析师不引用被注入字段（`derived_series.chg_5d` / income_statement 营收格）→ **下一轮靶点：注入到产物 claim 实际引用的字段**（recompute 注册根优先，使 A3 值检查可拦）
- `mirror_narrative` ×3 ok + 1 void：MA.5 反转**首次进入产物**（de-void 成功），但两态均 caught（一致对，无机制信号）——A2 开关效应未显现，需结合逐态证据读
- `illegal_price` ×4 void：跑图产出的决策为 `action='watch'`（hold/watch 豁免价位要求）→ 注入不可施加 → **下一轮：筛选可执行决策（buy/sell）的标的/实例**

阳性对照：显著（n=76，b=56，c=0，exact p=2.8e-17）→ 本轮阴性结论不作废。
停止规则：总体不一致比 **0.374** 落健康区间；终裁 **180 条 < 300** 上限。
终裁清单已更新为 **round 2 证据增补版**（新增逐态 `polluted_present` / `flagged` 列，便于逐条裁定）。

## 8. 下一轮（round 3 候选）

1. `value_error` 靶点改为「产物 claim 实际引用字段」（保证到达 + A3 可拦）——**真跑型，待批**（§10 已把离线侧做完）
2. `illegal_price` 可执行决策（buy/sell）筛选——**真跑型，待批**
3. A6/A7 可测面定义 ——**已完成**（§10：可追溯性面 + 输入侧面）
4. 终裁回填 → `escape_rate` 出数（当前 `rate=None`，口径纪律；清单已按 round 3 重生成）

## 9. 终裁材料补齐（2026-09-16，round-2 证据审读）

round-2 交付的终裁清单四列逐态证据**交付时全空**（`6f3fcc9` 只加了列头未填值）——人工
无从判读。本轮补齐为可判材料：库侧 `evals/causal_ablation/adjudication.py` + 薄壳
`tests/scripts/p1_adjudication_material.py`（20 条单测，`tests/evals/causal_ablation/test_adjudication.py`）。

- **单元级清单**（180 行，`2026-09-16-p1-adjudication-worklist.csv`）：四列证据回填，
  新增 `on_flag_bucket` 列（真拦 vs 伪拦截可分辨）；已填的人工裁定按 `unit_id` 继承，
  重生成不覆盖人工终裁。
- **案例级清单**（50 行，`2026-09-16-p1-adjudication-cases.csv`）：同标的同型的确定性实例
  **完全相同**（注入载荷与污染 claim 一致）→ 折叠为案例：**判 50 次而非 180 次**
  （period_shift 每标的 2 实例，其余 4 实例；实例数之和 == 清单行数，有单测守口径）。
- **机器预读列**（证据整理，SHALL NOT 读作终裁）：23 真逃逸候选（反事实型）/ 10 无拦截面 /
  10 自证出处 / 5 未产生有效差异 / 1 伪拦截 / 1 覆盖缺口。

### 9.1 证据审读新发现的注入构造缺陷（登记为 round-3 修复项）

| 缺陷 | 实证 | 影响 |
|---|---|---|
| `direction_error` 半数翻转是**空操作** | stated 自带符号（如 -1.21），翻成 positive 后 `eff = stated×(+1)` 与原来相同 → A3 PASS（delta≈0）；只有 stated 为正、翻成 negative 才产生真矛盾 | 20/40 单元不携带机制信息 |
| `unit_error` 的 ×1e8 在部分标的上**恰是合法换算** | 002415 原值 141.95（亿元）×1e8 = 1.4195e10（元），与真值相对差 2.6e-5（容差内）→ PASS | 4/40 单元是无效污染（终裁应判「误报」） |
| `fabricated_event` **自证出处** | 假新闻与假引用同源注入（`mutated_fields` 含 `snapshot.news_list`）→ 回声匹配必然 PASS | 10/10 案例不携带 A6 信息（A6 可测面须与注入改造同做） |
| **伪拦截** | 002594 的 on 态 FAIL 桶为 `path_unresolvable`（field_ref 本就解析不出），与翻转无关 | 1 案例不得算机制功劳（`on_flag_bucket` 列即为可分辨而加） |

**这些缺陷不改变 round-2 的停止规则判词**（0.374 仍落健康区间），但改变分型的可读性：
按可归因子集重算，`direction_error` = 20/20、`unit_error` = 36/36（均贴 too_hard 边界）——
即这两型的可检出污染**全靠 A3 单点**（同 period_shift 之于 A2），round-3 需按「双形态」思路
补非 A3 可拦形态，而不是硬扩样本。

## 10. Round 3（注入构造重校，2026-09-16，离线腿零 LLM）

§9.1 的四项构造缺陷已修复（库侧 `evals/causal_ablation/pilot_runner.py` + `injection.py`，
单测 316 条），离线腿重跑：`reports/ablation/p1-round3/`（200 单元 / **0 LLM 调用** /
0 void / 0 error；`--legs offline`，未动真跑腿）。

| 型 | round 2 | round 3 | 变化说明 |
|---|---|---|---|
| direction_error | 0.500（半数是空操作） | **1.000** | 只选「翻转真改变有效值」的 claim（`_effective_direction_flip`，判据走校验器单一实现） |
| unit_error | 0.900（4 例是合法换算） | **1.000** | 倍率按「是否被归一化吸收」挑选（`_effective_unit_error`）：1e8→恰等于真值、1e4→「万」、1e6→percent(×100) 均被吸收 → 退到 1e3 |
| fabricated_event | 0.000（无面 + 自证出处） | **1.000** | 改为**只注入 claim**（不写 news_list）+ **可追溯性面**（拦截 = 未静默放行，判定非 PASS） |
| period_shift | 0.500 | 0.500 | 不变（双形态仍有效） |
| stale_macro | 0.000（无面） | 0.000（校验面） | 改读**输入侧面**：presence 检查 40/40 ON 有「数据滞后」告警、0/40 OFF → 判「机制生效」 |
| **合计** | 0.374 | **0.700** | 落健康区间；阳性对照显著（b=80 c=0 p=1.65e-24）；0 void / 0 error |

**读法（重要）**：direction/unit/fabricated 三型的 1.000 = **单点依赖**（该污染只有唯一机制能
拦下），是结构事实而非测量缺陷——**不得按「too_hard → 再降强度」处理**：污染现在已是有效的，
再降就退回空操作（round-2 的教训）。stale_macro 的 0.000 只反映「校验链上无此面」，其读数由
`input_side_evidence` 取代。

**可测面分型**（新增 `FLAG_SURFACE_KIND`，报告 `by_type[*].flag_surface`）：`fail`（A2/A3/A5 型）/
`traceability`（A6：PASS↔UNVERIFIABLE）/ `input_side`（A7：context presence）/ `none`（无面 → 盲区）。
`blind_spots` 现只列 `none` 型（round-3 离线批次为空）。

**清单变化**：终裁清单 180 → **140 条**（stale_macro 40 条移出清单：输入侧面无「拦截/逃逸」
语义，判读走 presence 证据；`worklist_excluded` 显式披露，不静默减条）；案例级 **40 条**
（10 标的 × 4 型），机器预读**全部为「真逃逸候选（反事实型）」**——不再有空操作/无面/自证出处单元。
两份 CSV 已按 round-3 台账重生成（`--resume reports/ablation/p1-round3/resume.json`）。

## 12. Round 3 真跑腿（value_error 靶点重校 + illegal_price 决策复用，2026-09-16）

两标的（600519 / 000001）× 两型 × 4 实例 = 16 单元；实耗 **119 次调用**（申报 ~500；
`reports/ablation/p1-round3-real/`）。

| 型 | 单元 | 结果 |
|---|---|---|
| value_error（8） | 靶点改为 claim 实际引用且可重算的字段（commit `111822b`） | **2 个有效 b 对**（000001-value_error-0/-3：开态拦下、关态逃逸）；2 void（两态均未到达产物）；2 一致对；1 反向对；1 一致对 |
| illegal_price（8） | 决策复用（commit `f86d970`） | 两标的决策均 `action='watch'`（hold/watch 豁免价位要求）→ 8/8 void；**每标的只跑 1 趟全图**（其余 3 实例 `llm=0/runs=0`）——复用生效，**成本 4×→1×** |

### 12.1 新发现（两条，均已登记）

1. **单态未到达被读成「拦下」（可比性缺陷）**：`600519-value_error-0` 关态 `polluted_present=False`
   被判 caught，但那是**该趟 LLM 采样未引用该字段**（污染未到达产物），不是任何机制拦下——与离线腿
   `path_unresolvable` 伪拦截同类。真跑型配对的可比性前提 SHALL 是**两态都到达产物**；当前实现按
   spec letter「不含该值 = 拦下」记，会把采样噪声记成拦截。**修法**：两态未同时到达 → 判
   `void_injection`（不含拦截信息），不计入配对。
2. **重算拦截依赖 `claim_type=computational`（结构性覆盖缺口）**：冻结重放首批（600519-value_error-0）
   里，分析师把 `cashflow_metrics.FCF.2025` 标成 `claim_type="numerical"` → A3 走 `_verify_numerical`，
   真值**从被污染的 state 解析**（`ground_truth == 污染值`）→ PASS，污染结构性不可检；只有标成
   `computational` 才走 `_COMPUTATIONAL_RECALC` 重算路径（从原始报表重算 → 能抓偏差）。
   即：**能不能拦住 value_error，取决于分析师给 claim 贴的标签**（LLM 决定的路径选择）。
   处置候选：① 校验器对「重算注册表根」的数值型 claim 也强制重算；② prompt 要求指标字典引用标
   `computational`。二者都是产品侧改动，登记待决策。
3. ~~**A5 在真实决策分布上无可测面**：3 标的全产出 `watch`~~ → **已由 §14 推翻**（20 标的实测可执行率 25%，该条系小样本巧合；保留原文以留痕，不得再引用本条的「暴露度≈0」）。这不是注入缺陷而是**暴露度事实**（默认 query 下决策层极少给出可执行价位）。
   A5 的读法要么改按 B4 遥测（价位合法性打回率/修正触发率）改读，要么接受"暴露度≈0"作为结论；
   登记为待决策项。

## 11. 人工终裁结果与主指标出数（2026-09-16）

**终裁结论**：40 个案例（140 个单元）**全部确认为真逃逸**（人工裁定，按 §10.1 三问逐条口径；
两份清单的 `human_verdict` 列已回填）。stale_macro 40 单元不进清单（输入侧面，走 presence 证据）。

**逃逸率出数**（`rate` 从 `None` → 数字；`adjudicated=140`）：

| 读法 | 数 | 含义与信息量 |
|---|---|---|
| ① 关态逃逸率（**现行实现**：已终裁单元中关态逃逸占比） | **140/140 = 1.000** | **退化读数**——清单定义即「关态逃逸」，分母全是逃逸候选；它读的是「关掉机制后污染全漏」这一反事实 |
| ② 机制开启时的残留逃逸（开态逃逸 / 全部 ok 单元） | **0/200 = 0.000** | **信息量所在**：每个污染型在机制开启时**无一逃逸**（与三型不一致比 1.000「单点依赖」互证） |
| ③ 跑批口径（关态逃逸 / 全部 ok 单元） | **140/200 = 0.700** | 与本批总体不一致比 0.700 同源（两态恒为 caught/escaped 时二者相等） |

**待 owner 决策（第 3 项：分母语义）**：三种读法对应三个分母，**建议以读法 ② 作主指标**
（它回答"防线有效性"），把读法 ① 降为机制必要性的辅证——现行实现（①）在本批为退化值（=1.000），
不改口径则主指标无信息量。选定后改 `escape.py::escape_rate` 的分子分母语义 + 复现测试。

### 10.1 终裁清单读法（图例）

**三问**：① 污染真进产物了吗（`polluted_field` + `polluted_values` 非空、`off_state=escaped`）；
② 开态那次拦下是真拦吗（`on_flag_bucket` ∈ {`direction_mismatch`/`value_mismatch`/
`semantic_period_mismatch`/`internal_inconsistency`}；**`path_unresolvable` 是伪拦截**——FAIL 来自
claim 本就解析不出，与污染无关）；③ 关态真逃逸吗。三条成立 → 填「真逃逸」。
`instances`/`unit_ids`：同一标的同一型的实例**完全相同**，判一次、组内套用。

**看起来奇怪但正常的格子**（不是漏填，是结构如此）：

| 现象 | 原因 |
|---|---|
| `period_shift` / `002415-unit_error` 的 `ground_truth`/`delta` 为空 | **语义类检查**（A2 期次检查「申报期次在数据里存在吗」、内部回声检查「值与 claim 正文自相矛盾吗」）只报「不匹配」，不做数值比对 → 无真值/差值可报；拦截证据看桶名（`semantic_period_mismatch` / `internal_inconsistency`） |
| `fabricated_event` 的 `on_verdict=UNVERIFIABLE/-` | A6 可追溯性面：该机制没有判 FAIL 的能力，"无出处的 claim 不静默放行"即算拦下（见 §10 可测面分型） |
| `direction_error` 的 `delta` ≈ 2×`ground_truth` | 污染是方向标签翻面，有效值变号 → 原始差 = 2 倍真值（正常算术，非异常） |
| `002415-direction_error` 的 `delta=18.71` 与 `ground_truth=0.1852` 不同单位 | claim 面值是**百分数**（18.52）而真值是**小数**（0.1852）——方向冲突分支在校验器里**先于单位/百分比归一**返回原始差（`abs(gt - eff)`），故该 delta 只能读作「方向冲突成立」，**不能读作偏差幅度**（按同一单位算应为 0.37）。判词不受影响（桶 `direction_mismatch` 正确）；此为校验器侧观测量缺陷，已登记待处置 |
| `unit_error` 中 `002415` 的桶是 `internal_inconsistency` | 值被放大到与 claim 正文自相矛盾，先被内部一致性抓到（同属 A3 校验链，仍算真拦） |
| `polluted_values` 曾是方向型的申报对照值 | round-3 实测缺陷（commit `875b6c5`）：方向型漏写真实标记 → 已修为翻转后的方向词，并加 `polluted_field` 列 |

**spec 更新（已落 delta）**：新增 `### Requirement: 可测面分型与注入有效性`——四类可测面
（`fail`/`traceability`/`input_side`/`none`）的定义与读法边界、注入有效性要求（空操作/等价换算/
自证出处三类形态 SHALL NOT 作为实验单元）、输入侧面不冒充拦截；`openspec validate --strict` 通过。

## 13. owner 裁决落地与修法 (a) 摸底（2026-09-16）

**裁决①（value_error 本轮结论 + 立项修 a）**：本轮结论 = **拦截率 ≈0，根因 claim_type 标签路由**
（5/5 载体 claim 标 `numerical` → 校验器从被污染 state 取真值 → 恒 PASS）；「待修复后重测」单独立项。
修法选 (a)（校验器对注册表根的 claim 无视标签一律重算），不修 (b)（prompt 强制标签）——原则：
**校验深度不得由被校验对象的自我声明决定**。
**威胁模型诚实边界**：context 指标字典注入在生产对应「state 本身被污染」，其正经防线在数据层
（三表勾稽/多源回退），citation 链路拦不住它属分工使然；本缺口真正的生产暴露面是
**LLM 自算值标成 numerical → 跳过重算 → 自算错误逃逸**。严重度按此定级。

**裁决②（主指标口径）**：主指标 = **开态条件逃逸率**（分母 = 开态下污染到达机制面前的单元）；
辅报 暴露率（到达产物 ÷ 全部注入单元）；对照 关态穿透率（阳性对照角色）；跑批口径降为附录
透明度指标。`escape.py` 与报告的三栏结构按此改造。

**裁决③（归档 + incident）**：本批结论归 v2.2；claim_type 路由缺口登记为 incident
（消融设施发现的第一个机制存在性缺陷）。

### 修法 (a) 摸底三问

1. **改动面**：路由点在 `citation.verify_claims` 的 claim_type 分派（numerical → `_verify_numerical` /
   computational → `_verify_computational`）。**但不能直接把 numerical 送进 `_verify_computational`**：
   后者只做「重算 + 0.5% 相对容差」，**没有 `_verify_numerical` 的单位/百分比候选归一**（面值亿元/百分数
   vs 重算原始值）→ 会造出一批**跨单位误报**。正确形状：**重算得真值，再走既有比较机制**（把
   `_verify_numerical` 的比较段抽为共用实现，口径不复制）。
2. **流量与 fixture 覆盖**：注册表 10 个根；10 标的中**挂在注册表根但标签非 computational 的 claim = 324 条**
   （technical_indicators 116 / profitability_metrics 53 / derived_series 49 / solvency_metrics 34 /
   cashflow_metrics 29）——修复后这批**全部新走重算**，而现有 fixture 覆盖的是重算函数本身（spec §72
   要求每根键独立 fixture），**未覆盖「重算真值 × 面值口径 stated」的组合** → 需先补这类 fixture 并做
   误报基线测量，再开闸。
3. **spec 影响**：`openspec/specs/citation-verification/spec.md` §72「计算型声明重算注册表全覆盖」是
   按 **claim_type=computational** 写的；修 (a) 改变路由规则 → 该 Requirement 需 MODIFIED，**走 delta**
   （主规范库不得手改）。

**修复后的验证（复利兑现）**：冻结 claim 已落盘（`reports/ablation/p1-frozen/frozen/`），修完 (a)
**零 LLM** 重放即出「修复前后拦截率」对比，同时充当该修复的回归测试。

## 14. A5 暴露度结论修正（2026-09-16，20 标的实测）

**推翻 §12.1 第 3 条的「暴露度 ≈0」**：该结论基于 3 个标的的观察（600519 ×2 轮 + 000001），
是**小样本巧合**。20 标的全图真跑实测（`tests/scripts/decision_action_distribution.py`，
382 次调用，19.1/标的）决策动作分布：

| 动作 | n | 占比 |
|---|---|---|
| watch | 14 | 70% |
| buy | 4 | 20% |
| sell | 1 | 5% |
| hold | 1 | 5% |
| **可执行（buy/sell）** | **5** | **25%** |

可执行标的：000858, 601398, 000333, 601012, 600887。

**影响**：① A5（价位 sanity）**并非无可测面**——这 5 个标的可直接建 `illegal_price` 单元测拦截率；
② D9 的「A5 结论两联」第一条（暴露度≈0）应改为「暴露率实测 25%」；③ 族 B 的 B3（数字出处率）
分母不再趋零，B5 的区分度问题也相应缓解——MDE 反算输入更新为 25% 而非 ~0。
④ 同时取得全图真跑的**成本基线：19.1 次调用/标的**（此前只有 3 标的的零散观察）。

## 15. A5（illegal_price）测量批：本轮**不可读**（测量面缺失，已修）（2026-09-17）

5 个可执行标的（000858/601398/000333/601012/600887）× 4 实例 = 20 单元，实耗 **95 次调用**
（决策复用生效：5 趟全图，其余 15 单元 0 调用；meter 交叉核对一致）。

| 结果 | n | 说明 |
|---|---|---|
| void：新跑一趟决策变 `watch`（不可注入） | 16 | LLM 重采样 → 上轮 buy/sell 本轮未必复现（暴露率 25% 是估计不是保证） |
| ~~ok（escaped）~~ → **实为测量面缺失** | 4 | `000333`：ON 态 `price_check = {"result": "pass", "note": "price_levels 不可用（unknown），跳过校验"}` |

**关键判定**：那 4 个"ok/escaped"**不是 A5 没拦住，是它根本没跑**（消费面 `price_levels` 缺失 → sanity 跳过）。
按本项目一贯纪律（incident 028 / round-3 反复修的同类误读：「没测到」≠「逃逸」），已修：
`_run_post_graph_decision_case` 检出「ON 态 sanity 因价位面缺失而跳过」→ **判 void**（与「注入不可施加」
/「暴露未实现」同级），SHALL NOT 记作逃逸；回归用例 `TestPriceSurfaceMissing`。

**A5 现状**：仍是唯一**从未真正测过**的机制。前置修正：让 illegal_price 单元在**带 `price_levels` 的
真实全图状态**上跑（材料快照缺该键 → 需在跑批时保留全图产出的 `price_levels`，或用价格面完整的快照）。
暴露率已实测 25%（20 标的），有 5 个可执行标的可用。

## 16. A5（illegal_price）实测：拦截率 1.000 —— 最后一个未测机制落地（2026-09-17）

**根因修复**：`price_levels` 由 compute 节点产出（`compute.py:60`），但材料落盘时 `snapshot_keys`
漏存该键 → 材料快照无价位面 → sanity 每次"跳过校验"（§15 的 4 个伪 ok 即此）。修复：① 快照键补齐
（未来材料自带）；② 既有材料用自身 `kline` 现算回写（**同一实现** `calc_price_levels`，非复制口径）。

**重跑结果**（5 可执行标的 × 4 实例 = 20 单元，实耗 **95 次调用**，meter 交叉核对一致）：

| 结果 | n | 说明 |
|---|---|---|
| **ok：on=caught / off=escaped** | **16** | 4 标的（000858/000333/601012/600887） |
| void：重采样变 `watch`（不可注入） | 4 | 601398（暴露率 0.25 是估计，单样本会翻） |

- **拦截率 = 16/16 = 1.000**；**不一致比 = 16/16 = 1.000**（b=16, c=0）——A5 是这四标的上
  **价位幻觉的唯一防线**（关掉即全放行）；
- ON 态 sanity 的真实判词（落盘可查）：`价格关系违规（long 要求 stop<entry<target，实际 76.67/69.7/73.2）`；
- 至此**族 A 的 A2/A3/A5/A6 全部有实测读数**。**A1/A4 尚无专门单元**（各批 mechanism_id 分布：A2/A3/A5/A6/A7，无 A1/A4）——原写「见各自腿」系过度声明，此处更正：A1（确定性指标注入）需「无 compute 节点」的变体才能测、A4（单点修复回路）需修复回路 on/off 单元；两者**属本轮矩阵外**，登记为下一轮补测项（见 BACKLOG）。

**口径注**：本批是"决策层后置注入 + 同一决策两态校验"（确定性配对，1 趟图/标的）；不同标的下
决策动作会重采样变化（601398 本轮即从 buy 变 watch），因此 A5 的**拦截率分母 = 暴露实现单元**，
这正是三栏口径「机制只对它见到的负责」的落地。

## 17. A4（单点修复回路）补测：数值 20/20 修对，但流水线只记账 4 —— 族 A 最后一格落地（2026-09-17）

**背景**：A4 此前**从无专门单元**（§16 已更正）。登记主张 =「没有修复回路，残余 value_mismatch 滞留正文」，
登记主指标 =「修复前后真 FAIL 率差」。本批首次建单元，预登记 `evals/ablation/preregister/2026-09-17-p1-a4-repair.md`。

**为什么不能复用既有 `unit_error` 注入器**：离线注入只改 claim 不改正文，而回路第一步是
`citation_repair.locate_sentence`（按 `claim.stated_value` 在正文里找出错句）——正文仍是原值 →
定位必然落空 → 回路连「尝试」都没有。实测：`locate_sentence` 返回 None，20 单元里 **0 次** LLM 调用。
故新建「正文与 claim **一致**」的注入形态（`evals/causal_ablation/repair_a4.py`；不动 8 类污染矩阵）。

**观测装置先修（4 处，均为本批自己的假读数）**：① claim-only 注入 → 定位落空（上述）；
② 重校验仲裁按**全库**判而生产按**分析师**判（`citation_node`）→ 会把 100% 的修复判为失败
（全库口径 `reverify_global_all_pass = 0/22`）；③ `_fmt_like` 沿用原小数位（7.5→9.375 写成「9.4」）
→ 写出的 token 定位不到，20 单元里 3 个没调到 LLM；④ 注入改写落在全篇**首个同值处**而非定位句
（600276 的 CPI 值 1.0 与「2026Q1」里的 1 同值）→ 句子级读数失真。四处修完才读数。

**结果**（20 标的 × 1 注入单元 + 2 自然命中单元 = 22，实耗 **22 次调用 / 11.6k token**，本轮唯一成本）：

| 读数 | 值 | 说明 |
|---|---|---|
| 关态（回路补丁 no-op）污染值滞留 | 20/20 | **构造性确定**（无 LLM、无随机性） |
| 开态改写的值是否修对（单条校验） | **20/20** | `target_value_correct`；GLM 全部回填到真值 |
| 终稿是否仍含错值 | **0/20** | `error_left_in_report`（= 未改写 或 改写值没修对） |
| 目标句清除（句子级） | 19/20 | 1 例为「同值在句中合法复现」的检测噪声（000651：环比 0.5→0.4 改了，同句 7 月 CPI 0.5% 合法保留） |
| **流水线记账**（`reverify_all_pass`，即登记主指标口径） | **4/22** | 只有「该分析师 claims 全 PASS」才记账 |
| 真 FAIL 率差（登记主指标） | **0.182**（1.000 → 0.818） | 低于预登记 80% 阈值 |
| 自然腿（离线复算出 2 个稀疏 value_mismatch） | **0/2 参与** | 定位落空（正文值型 ≠ 申报值型），连 LLM 都没调 |

**分桶归因（未记账 18/22 的差额不是「没修对」）**：

| 桶 | n | 判读 |
|---|---|---|
| 真修复（改对 + 仲裁通过 + 记账） | 4 | 该分析师**没有其它 FAIL** 的 4 例（601398/601012/000651/002027） |
| 改对但仲裁未过（同分析师另有 FAIL） | 16 | 值已改对（单条校验 PASS），但同分析师还有 1–6 处其它 FAIL → `all_passed=False` |
| 自然腿无产出（定位落空） | 2 | 未注入、正文本值定位不到 → 无改写、无调用 |

**生产语义要点（决定这条机制到底值多少）**：`citation_node` **先回填正文再重校验**，仲裁失败
**不回滚** —— 故「不记账」≠「正文没改」。在 `CITATION_AUTO_RETRY_ENABLED=False`（incident 026 阶段 0）
的当前配置下也没有全量重试兜底，终稿里的错数字只能靠回路改掉——本批实测**改掉了 20/20**。

**登记主张判定**：注入腿上「没有修复回路 → 残余 value_mismatch 滞留正文」**成立**
（关态 20/20 滞留 → 开态 0/20 留错）。但**机制收益在生产遥测里不可见**：`value_mismatch_repaired`
只在仲裁通过时 +1，本批低估 16/20（80%）——已登记 incident 029。

**边界（不得外推）**：① 注入腿是「正文与 claim 一致」的受控形态，自然分布的稀疏失败未必如此；
② 自然腿 2/2 定位落空，说明回路对**值型不一致**（正文 亿元 / 申报 元）的 claim 无效——自然腿
逐条案例已导出 `tests/validation/2026-09-17-a4-natural-cases.csv`，**待人工终裁**（真错误?/误报?）；
③ 关态不是生产反事实（生产缺席 A4 时本应回退全量重试，该路径本批不测，且当前配置下重试已停用）；
④ 有效覆盖面：1070 条数值 claim → 765 条可产生 value_mismatch（71.5%）→ 757 条正文可定位（99.0%）。

**成本申报**（真跑型，逐轮）：`19 + 22 + 22 + 23 + 22 = 108 次调用 / 58.8k token`
（前 4 轮含观测装置调试，读数已被 §17 修订版取代；**最终批 = 22 次调用 / 11.6k token**）。

## 18. A1（确定性指标注入）补测：主指标不可测（分母塌缩），但机制价值落在产出面（2026-09-17）

**背景**：A1 是族 A 最后一格待测机制（登记主张 =「没有代码算指标，LLM 会自算并算错」，失败模式 = 计算幻觉）。
设计：OFF 态 = 把 `compute_metrics` 的**输出键**从分析师所见快照里剥掉（原始三表 / kline / 新闻 / 宏观原样保留），
ON 态 = 原样；两态的 claim 判定用**同一把尺子**（完整快照 + 生产 citation 链）。10 标的 × 2 态，实耗 **102 次调用**。

**预备修复（观测装置，见 incident 030）**：建单元时的零成本预检发现 20 个材料产物**全部**缺 compute 输出键
（每标的 5–6 个，共 115 键：anomalies / garp_result / health_score / price_levels / risk_metrics / traffic_lights——
其中 `price_levels` 是第二次踩）。根因 = 材料落盘的手抄白名单漂移 + `snapshot_digest` 记占位串 `"injected"`。
已修：白名单改排除式 + compute 输出完整性守卫 + 回填脚本（零 LLM，同一实现）+ 摘要回真。

**结果**：

| 读数 | OFF（无 compute 注入） | ON（注入在场） |
|---|---|---|
| claim 总量 | 388 | 606（**+56%**） |
| 引用 compute 产出根的 claim | 9（2.3%） | 355（**58.6%**） |
| 其中可重算根（主指标分母） | **4** | 319 |
| 可重算根的值级错误（`value_mismatch`） | **0/4** | **0/319** |
| 路径不可解析（`path_unresolvable`） | **52（13.4%）** | 1（0.2%） |

**判定：登记主指标（计算型 claim 数值错误率差）本轮不可测**——10 标的中 6 个因 OFF 态分母为 0 判 void，
其余 4 个各只有 1 条可重算 claim（0 FAIL）。触发预登记停止规则②（两态 claim 产出量差 > 3 倍），
按规则**先归因再读数**：

1. **分母塌缩的成因不是「LLM 自算算错」，而是「LLM 基本不自算」**：没有注入时，分析师几乎不产出
   可重算根的 claim（319 → 4，-98.7%），改为回落到原始报表/指标表路径（`financial_indicators` 103 条、
   `income_statement` 57 条）。**A1 的登记主张（自算→算错）在本实验里没有样本**——不是被证伪，是没有发生。
2. **机制的真实价值落在产出面**：注入在场时，58.6% 的 claim 锚在代码算出的指标上（可重算、可独立核对）；
   缺席时这一比例掉到 2.3%，而**路径不可解析率从 0.2% 涨到 13.4%**——52 条不可解析里 47 条是
   `financial_indicators.<年>.<指标名>` 形态（校验器的解析器不认这个形状，两态都不可解析：ON 态里
   分析师会优先引 `profitability_metrics` 的可解析路径，OFF 态被迫回落才暴露）、5 条是已被剥掉的 `growth_rates`。
   换言之：**没有注入，报告里的数字失去可核对的锚**（不是"算错"，是"无从判对错"）。
3. **claim 总量 -36%**：注入缺席时报告可写的内容也变少（可引用的先算指标没了）。

**边界（不得外推）**：① OFF 态剥的是 `compute_metrics` 在该快照上**复算得出**的 14 键；`quarterly_trend` /
`peer_comparison` / `relative_valuation` 因原始输入（`quarterly_income` / peer 数据）未落进快照而无法复算，
**仍留在 OFF 态**——OFF 态是「大部分派生指标缺席」，不是「全缺席」（OFF 态里仍有 36 条 `quarterly_trend` 引用）。
② 主指标的「0 FAIL」建立在 4 条 / 319 条两个极端分母上，**不得**读作「两态错误率相同」。
③ 4 配对单元的簇 bootstrap CI [0, 0] 无分辨率（报告内已加口径注），不作结论依据。

**结论与登记表修正**：A1 的 `effect_expectation` 按实测改写为「产出面塌缩 + 溯源面丧失」，
并注明「自算算错」这一形态在本项目当前提示词下**未出现**；若 owner 要测该形态，需另设
「要求分析师自行推导指标」的变体（登记为待决策项，见 BACKLOG）。

## 19. P2 族 B 开跑：材料腿 + code 腿读数（2026-09-17）

**材料腿**：层间对照 = analysts 臂（P1 既有产物）vs full 臂（本批真跑 20 标的，同一份快照）。
状态捕获按**排除式**（incident 030 教训），pickle 全量 + JSON 摘要，逐标的续跑。实耗 **19 次调用/标的**
（与 P1 成本基线一致；full 臂累计 380 次调用）。

**B3 风控数字出处率（code）= 0.952（20/21）**
- 7 标的产出数值执行参数（entry/stop/target 共 21 个），其中 20 个能在上游池（工具参考带 ∪
  分析师 claim 值 ∪ 派生指标 ∪ 上游计划）找到同值来源；唯一无出处：**002594 的 stop_loss = 81.2**。
- 口径：容差复用校验器 `value_close`（不另立一套）。

**B4 sanity 首判（code）= 打回 1/7**
- 分母 = 需申报价位的决策（buy/sell）7 只；其余 13 只 hold/watch 不进分母（`skipped_reasons` 单列
  「hold/watch 无价位要求」，与「校验通过」不混读）。
- 唯一打回：**000333** —— `target 93.34 落在参考带 [69.3443, 92.5557] 之外`（sanity 在真实决策上
  确实开火）；修正触发 0 次。

**结构发现（改口径的原因）**：消融变体图**不含 sanity 节点**（`build_variant_graph` 无 validate）→
捕获材料里 `price_check / derived_metrics` 恒为 None，B4 读不到。改用生产**同一实现**
`validate_trade_prices` 在产物决策上补算。**与真跑主图的差异如实披露**：补算是单次校验，
不含生产主图的「打回 Trader 重出一次」回路（`price_check_feedback`）——故 B4 读的是**首判分布**，
不是完整回路的打回率；`correction_rate = 0` 因此不含修正路径。

**分辨率声明**：B3 的 n=7（21 个参数）、B4 的 n=7 → 按 §4 的 MDE 表**无分辨率**（n<10 时
`mde_paired_binary` 返回 None）→ 两腿均**只作描述性读数**，不作显著性裁决；扩样本出路见 BACKLOG。

### 19.1 判定腿读数（B1/B2/B5，2026-09-17，均 provisional）

| 腿 | 判定方式 | 判定行 | 读数 | 状态 |
|---|---|---|---|---|
| B1 风险点吸收率 | nli | 196（20 标的，全空方论点） | **0.786**（per-ticker 0.50–1.00） | provisional，待校准门控 |
| B2 交锋修正率 | judge | 111（20 标的，≤4 行/标的去重后） | **0.198**（per-ticker 0.00–0.40） | provisional，待校准门控 |
| B5 pairwise 盲评 | judge K=3 | 20 对 | **full 20 : analysts 0 : tie 0**（全 3:0） | provisional；**口径需修**，见下 |

零解析失败（196/111/20 全部拿到结构化判定）；抽检判词具体可核（如 002594「单季修复不足推翻
净利连降」判为修正成立；600519「反驳未回应毛利率/ROE 指标」判为未修正）。

**B5 的 20:0:0 是「整齐得可疑」，先解释再放行——解释结果是：本口径是同义反复，不得读作层价值证据。**

- 对照结构不公：analysts 臂**没有决策章节**（无 Trader/风控辩论/FM 层），full 臂有；而 B5 的判据正是
  「哪份**更有助于决策**」——让 judge 在「有决策 vs 无决策」之间选，必然全选有决策的那份；
- 佐证：两臂报告长度 2.9–3.5k vs 7.7–8.6k 字符（2.4 倍），且 full 独有决策/价位/风控段；
- 位置偏倚检验**通过**（9 对 analysts 先展示、11 对 full 先展示，full 全胜）→ 不是位置效应，
  是判据本身选不出信息；
- **修正方向**：B5 的两臂必须都含决策层（预登记 §8 的已知劣化变体 `plus_debate` vs `full`，或
  「裁掉一层但保留 Trader」的变体）。当前结果只证明「带决策层的报告看起来更有决策价值」。

**B1 的可读面**：判定集是**全部空方论点**（未按阈值预筛），故 0.786 = 空方论点整体吸收率；
落到登记主指标「**新增**风险点的吸收率」还差阈值标定（阈值敏感表见 §19 的 code 侧读数注释：
600519 在 0.35 下 12/12 全「新增」而字面相似度仅 0.06–0.20）。**标定前不得把 0.786 读成主指标**。

**成本与装置教训（如实申报）**：
- 判定调用：有效 **367 次**（B1 196 + B2 111 + B5 60 票），另有 **160 次因脚本崩溃丢失**（首轮
  `calibration_sample` 引用错模块，判定结果未落盘）→ 合计 ≈527 次；B5 的 60 次靠缓存迁移找回。
- 已修：① 判定**即时落盘** `judged-{b1,b2,b5}.jsonl` + 按 `unit_id` 回读（重跑零重烧）；
  ② `run_b5_pairwise` 保留传入 `unit_id`（丢了会让缓存匹配不上）；③ 判定行按 `unit_id` **去重**再聚合
  （同一 rebuttal 目标被多条消息反驳 → 不去重分母虚高 1014 vs 实判 111）。
- 校准材料：`tests/validation/2026-09-17-p2-calibration-b{1,2,5}.csv`（20% 确定性抽样，
  `human_label` 列留空待人工标注）；一致率 ≥0.80 才允许上述读数进结论。

### 19.2 校准收口：B1 有条件转正 / B2 待人工复核 + 字面新增阈值判死（2026-09-18；同日更正）

> **2026-09-18 更正（已被 v3 取代）**：首版误将 B2 判定腿一并"转正"，曾回退为未标注；
> **v3 终局**：owner 就 4 条分歧行逐条裁决（+1 行主动裁决）后重跑门控通过——见下方门控段。
> B1 锚强度同步降级：owner 为「深度参与口径制定 + 8 行逐条裁决 + 其余行批量认可预填稿」，
> 非独立逐行标注——一致率含机器-机器成分，按采样协议 v2 下一腿补独立高把握抽查后再终定。
> owner 首轮独立判读曾在 5 行上与预填不同（含 4 行把近原文回收判为「新增」）；终版 owner 改为
> 全量照预填——首轮判读保留为**标注人敏感性**来源（新增率首轮 0.55 / 终版 0.45）。

**门控**（`tests/scripts/p2_calibration_apply.py`，判词 JSON `reports/ablation/p2/calibration-closeout-20260918.json`）：

- **B1 吸收判定一致率 0.875**（40/40=预填稿全文照录，≥0.80）→ **有条件转正**（锚强度见上方更正注；判词 v2 `calibration-closeout-20260918-v2.json`）
- **B2 交锋修正一致率 0.906**（32/32；判词 v3）→ **转正**。provenance：owner 裁决 5 行
  （4 条分歧行全部逐条裁决：3 行维持预填「是」、1 行 000001::b2::2 改判「否」站机器侧——
  机器的「宁严勿宽」被部分推翻部分确认），其余 27 行照预填落。终读修正率 **7/32 = 0.22**
  （机器 4/32 = 0.125，机器口径偏严经 owner 裁决确认）。判例方向（顶撞型修正）不变

**标注 provenance**：B1 = 机器（本会话）预判 40 行 + owner 认可预填稿（其中 8 行经逐条讨论裁决、5 行与预填不同）
（002594::b1::8 吸收 是→否；600887::b1::8 / 000651::b1::7 / 601899::b1::1 / 600887::b1::3
新增 否→是）。工具链：引用命中对照表（`p2_calibration_refs_scan.py`，全量 refs 逐条相似度 +
来源侧分账）、安全写入器（`p2_prefill_write.py`，用户列代码级只读——2026-09-18 一次预填稿
整体重建事故的整改）。

**分歧归因（8 条全系统性，无随机分歧）**：

- B1 的 5 条分歧中 4 条（000858×2、601398×2）是机器把分析师通道引用记成辩论功劳的**通道假阳性**
  （与 refs 扫描 analyst 顶命中 162/196 一致）；第 5 条（002594::b1::8）反向——机器判未吸收，
  owner 首轮独立判读亦为否，预填稿的「是」为孤例（已知弱项，下腿抽查重点行）→ judge prompt v2 头号候选 = **通道分账规则**：
  新增行只认新增截经非分析师通道（debate_*/risk_*/research_manager）进决策；新增截出现在
  analyst 标签条目 = 「是否新增」判错（自洽校验）
- B2 的 4 条全部是机器「宁严勿宽」漏判的**直接顶撞主张型**修正（000001::b2::2 拨备调节动摇
  企稳、002594::b2::1 否认拐点、600519::b2::5 配错价格顶撞周期定价、601012::b2::5 毛利率
  实证否定出清尾声）→ 候选 = 补「反驳是否直接针对原论点本身」判例

**B1 阈值标定 → 字面 Jaccard 路线判死**：40 行全区间扫描最佳 0.05 仅 0.65 一致率（阈值预测
新增 10 vs 人工 22）——字面相似度与人工「是否新增」结构性不匹配。处置：「是否新增」并入
判定腿，B1 主指标 = 判定腿双问；口径已按纪律先改 `docs/evals/metrics.md` §1.7（代码未动）。

**人工终裁读数（B1 标注样本 n=40，终版=预填稿全文照录）**：新增率 **18/40 = 0.45**（owner 首轮
独立判读为 22/40 = 0.55——边界带 0.45–0.55 照报区间）；新增点吸收率 **11/18 = 0.61**，回收点
吸收率 **20/22 = 0.91**（决策层偏接旧内容的倾向在终版数据中复现且更强）。owner 明确反馈「推断模糊、难以准确断定」——
边界行（覆写 5 + 低把握 2）≈ 18% → B1 增量率此后报**区间**；judge prompt v2 加「两问口诀 +
框架/判断判例表」。

**无源增量首证**（000858::b1::2「机构资金持续撤离」）：辩手输入仅 4 份分析师 summary
（`debate.py::_build_debate_context`，各 47–74 字）、零资金流内容；原话「均线空头排列…**说明**
机构资金在持续撤离」= 推断冒充数据，自标 `kind=data` 且无锚（anchors 仅 technical 两条），
与上游唯一资金流发现（板块净流入 11.09 亿）方向相反 → 全量溯源扫描待跑（kind=data 验正文
可溯源、有锚验可解析性）。

**遗留**：B5 重跑换两臂含决策层（plus_debate vs full）；无源增量全量扫描；judge prompt v2
两条候选；B3/B4 扩样决策（owner）。


### 19.3 judge prompt v2 阴性结果 + 新增维度封顶结论（2026-09-18，花费 160 次调用）

- **v2（双问+通道分账+顶撞判例一次全上）**：校准样本验证未过门——B1 吸收 0.825 / B1 新增
  0.750 / **B2 0.690**（较 v1 的 0.906 大幅回归）。门控拦下，v2 判定作废；默认回 v1
  （`778bba4`），模板归档为 `_*_TEMPLATE_V2` 只作迭代基线。教训：**双变量同动违反单变量
  纪律，失败无法归因**——后续 prompt 迭代一律走探针（`p2_b1_variant_probe.py`）。
- **v2a 探针（单变量：v1 吸收口径不动 + 仅加新增问题）**：吸收 0.875（与 v1 判定逐行一致，
  对照成立）；**新增 0.750**——与 v2 的 0.750 相同。失败 10 行全部是判断/框架边界行
  （人工自己也在此翻案：首轮新增率 0.55 / 终版 0.45）。
- **结论：新增维度的机器判定封顶 ~0.75，属维度固有模糊性而非 prompt 缺陷。** 处置：
  B1 新增率以**人工区间口径**为准（0.45–0.55）；机器新增判定只作辅助观测（一致率 0.75
  随读数披露），永不作门控指标。无源增量定版扫描改走 grounding 口径（bear 实际输入仅
  4 份摘要，`kind=data` 论点对摘要做可支撑性检查——比「对 16 条发现判新增」更接近 NLI），
  立项待登记，不在本轮跑。


### 19.4 无源增量 grounding 扫描（bg-v1，2026-09-18，64 次调用，provisional 待人工终裁）

**口径**（预登记 `2026-09-18-p2-b1-grounding-scan.md` 先行）：第 1 轮 ∧ kind=data 空方论点
（64 单元）× 辩手**真实输入**（4 份分析师摘要）的可支撑性——NLI 形态，绕开新增判定的边界
模糊（§19.3 封顶 0.75）。

**首读**：无源断言率 **8/64 = 0.125**，零解析失败。8 条无源清单（judged-bg-v1.jsonl）：
- 与 B1 人工判定互证：000858「机构资金持续撤离」（摘要零资金流且方向相反）、601398「股息率
  与无风险利率利差收窄」（B1 第 9 行新增=是的同一判断）、000858「资产回报率接近资金成本」
- 疑似「推断被当断言」边界行（待人工终裁）：000333「趋势存在反转风险」、002415×2
  （利润靠降本挤压/需求压制订单）、600036「盈利动能减弱」、300750「资金持续流出」

**采样协议 v2 第二实证教训**：判定模型自报把握 **64/64 全 high**——自报置信度零区分度，
分层改挂**判定正例**（无源行全审 8 + 有源行抽 10% 共 6 → 样本 14 行，
`2026-09-18-p2-grounding-calibration.csv` 人工列留空）。一致率 ≥0.80 前 0.125 只作
provisional 读数。


### 19.5 grounding 校准收口：一致率 1.000，无源率 0.125 转正（2026-09-18）

owner 终裁 14/14 行（正例全审 8 + 有源抽 6）**全部与机器一致（1.000 ≥ 0.80）**——含四条
推断式表述的边界行（000333「趋势存在反转风险」、600036「盈利动能减弱」、002415×2），口径
随之定型：**自标 kind=data 即要求事实断言有源；推断式表述冒充数据同样判无源**。

**终读：无源断言率 8/64 = 0.125（r1 ∧ kind=data 空方论点）**——每 8 条"数据论据"里有 1 条
含摘要无源断言；000858、002415 各占 2 条。8 条无源断言清单（owner 终裁确认，可作处置依据）：
机构资金持续撤离 / 资金持续流出 / 股息率利差收窄 / 资产回报率接近资金成本 / 利润改善靠降本
挤压 / 需求端压制政企订单 / 盈利动能减弱 / 趋势存在反转风险——共性是**从价格或摘要数字推断
出"资金流/利差/因果"类事实断言并自标 data**。

**处置登记（挂已终裁桶，按 SOP 可进入处置讨论）**：bear prompt 修改候选——kind=data 须逐断言
锚定，推断必须标 inference；属生产行为变更，走 OpenSpec 管线，不在评估工作流内动。


### 19.6 A4 自然腿终裁收口：2/2 误报（契约病），0 真幻觉（2026-09-18，owner 终裁）

- **601318::a4::natural:4**（报表 1347.78 亿 vs 校验 235.23 亿）：快照利润表**同名异义两列**——
  「归属于母公司的净利润」=1347.78 亿（分析师所用，官方科目全名）与「归母净利润」=235.23 亿
  （校验器 field_ref 解析命中）。分析师数值在快照中真实存在 → **误报（字段名歧义）**。
  次要疑点另记：正文「同比+16.9%」与两列算术（+6.4%）均不符，属另一条 claim 范围。
- **600276::a4::natural:17**（报表 0.6 vs 校验 49.8）：正文「PMI 环比回升 0.6 个百分点」算术
  正确（49.8−49.2=0.6）；claim 把**变化量填进水平值槽**（field_ref 指向 8 月 PMI 指数）→
  **误报（claim 值槽类型错填）**；修复回路定位落空是正确行为（文中无错数）。

**结论**：A4 自然腿的 value_mismatch 2/2 全为契约病、0 条分析师幻觉——与 incident 026 一脉
相承（校验器/契约误报 ≠ 幻觉，修契约不修分析师）。处置候选（挂已终裁桶）：
① 字段解析消歧（同名异义列须消歧或禁用短名）；② claim 抽取类型校验（delta 不得入 level 槽）。


### 19.7 B5 重跑（两臂均含决策层）首读 + eval-driven-contract-fixes 四件落地（2026-09-18）

**B5 修正口径首读（provisional，待校准门控）**：through_trader 臂（analysts+辩论+研究经理
+Trader，无风控辩论/FM）对 full——**full 19 : through_trader 1 : tie 0**，零解析失败，
A 位先出示 9:11 位置均衡（无位置偏倚迹象）；600030 的 through_trader 胜出表明评审非无脑
选边。**同义反复消除**（旧口径 20:0:0 的退化比较不复存在）：盲评首次表达真实偏好——
带风控辩论+FM 的完整链路在 19/20 对里被判更有助于决策。

**混淆披露与设计遗漏定性（2026-09-18，owner 审阅 4 对报告时发现并追问）**：两臂**上游
非冻结**——共用的是数据快照（digest 一致），分析师报告为各臂独立现跑（LLM 采样、
temperature 被端点剔除不可冻结），实测 4 对的两臂分析师发现**零字面重叠**（Jaccard 全
0.00）。**定性：设计遗漏**——预登记的混杂因子清单只枚举了数据侧与程序侧，生成侧（共享
上游层的 LLM 采样跨臂方差）从未进入清单，三关一致性检查也只覆盖 run 内、无跨臂检查。
故「唯一差异 = 风控辩论+FM」不严格成立，19:1 含上游采样噪声 + full 篇幅长一倍的观感
偏好（偏差有方向性，非统计可吸收的对称噪声），**不得作纯层归因**；B1/B2（同 run 内自比）
与族 A（同 run 注入）不受此影响。固化两条纪律：① 层增量消融默认冻结上游；② 校准增补
第四关「跨臂对照先验上游一致性」。干净化方案已登记：以 full 现有分析师产物为冻结上游，
仅重跑 through_trader 下游（≈120 次，待批）——两臂共享同一上游后重评 B5。材料 222 次 + 判定 60 次（owner
已批 ~360）。校准样本 4 对已导出（`2026-09-18-p2-calibration-b5-through-trader.csv`，
人工列留空）；一致率 ≥0.80 前读数维持 provisional。

**eval-driven-contract-fixes 四件全部落地**（openspec change `eval-driven-contract-fixes`，
TDD 全程）：
1. **同义列消歧**（c2fbac2）：canonical 等价多列并存取官方全名（最长列名）——601318
   「归属于母公司的净利润/归母净利润」假 FAIL 修复
2. **claim 值槽类型校验**（dfad1d3）：环比/同比措辞 + 量级差>10 倍双信号 →
   `claim_contract_error` 桶（不算幻觉、不触发修复）——600276 PMI 0.6/49.8 假 FAIL 修复
3. **修复记账按 claim**（a5c8b5b）：`value_mismatch_repaired_claims` 上线接管
   analyst_true_fail，旧 all_passed 口径 deprecated——incident 029 处置
4. **辩手断言级锚定**（ed2bc75）：三份辩手提示词逐断言锚定 + 推断冒充数据反例判例，
   deploy_prompts 已发布 production——grounding 无源率 12.5% 的处置

验收夹具：601318/600276 终裁案例即测试夹具（TestSynonymColumnDisambiguation /
TestClaimContractError）；全套受影响测试区（citation 族 + causal_ablation + debate prompts）
全绿；全套回归跑批中。


### 19.8 bear 提示词 A/B 验证：无源率 0.125 → 0.012（2026-09-18，20 次调用）

**设计**：同一批输入（缓存材料的 4 份分析师摘要、debate_history 清空 = r1 条件）只重跑
bear_debater 单节点（新提示词），与旧 grounding 扫描（bg-v1）对照——两侧唯一差异 = 提示词
版本。脚本 `p2_bear_prompt_ab.py`（按标的断点续跑，缓存 judged-bgpab-v2.jsonl）。

**结果**：

| | data 论点 | 无源 | 无源率 |
|---|---|---|---|
| 旧提示词（bg-v1 终裁） | 64 | 8 | **0.125** |
| 新提示词（A/B） | 85 | 1 | **0.012** |

- 8 条旧无源断言（机构资金撤离/资金流出/股息利差等）**全部消失**——反例判例直接命中
- 唯一新无源：600887「短期上行动能不足」（锚点挂 technical 两条，边缘表述，待人工复核是否维持）
- data 论点量 64→85：逐断言锚定没有抑制辩手标注（会锚了反而敢标），推断被正确分流
  （旧 r1 kind 分布 data 64/event 14/inference 23 作基线）
- **判定**：任务 4 处置**有效**，进入常规观测（下轮全量材料跑批的 grounding 复扫照做）。
**唯一残余行 owner 委托复核（2026-09-19）**：600887「短期上行动能不足」——摘要 technical
仅称「MACD 动能收敛、多空暂时均衡偏多」，且与「均衡偏多」方向相悖；「不足」是水平性评价
而非「收敛」的转述，按 600036「盈利动能减弱」同族先例判**无源维持**（有源=收敛事实，
断言=不足评价，摘要未断言）。注：该行出自 A/B 新提示词侧缓存（judged-bgpab-v2），非原
扫描——新口径下残差 ≈1.2% 的构成即此类边界表述。

**deploy 状态更正**：ed2bc75 提交信息称「已发布 production」**不实**——deploy_prompts 被
预检拦截（要求先 `sync_prompts.py --once` 收编），且本环境 Langfuse 离线（localhost:3000
拒绝连接，全会话 span export 失败同因）。本地 git 提示词（权威源）已就绪、运行时回退本地
已验证（A/B 即证）；**待 Langfuse 在线后执行 sync + deploy 两步**，此为遗留动作项。


### 19.9 B5 上游冻结重跑：纯层归因读数 full 17:3（2026-09-19，180 次调用）

**执行**：owner 批准后落地——① 冻结重跑（`p2_b5_frozen_rerun.py`）：以 full 现有分析师产物
为冻结上游，仅重跑 through_trader 下游链（bull/bear×2 → research_manager → trader → 渲染，
6 次/标的 × 20 = 120 次），材料存 `through_trader_frozen` 变体；② 第四关自检：20/20 的
两臂 analyst_reports **byte 级一致**；③ 判定 60 次（驱动换臂 `through_trader_frozen`，
缓存独立于噪声版）。

**纯层归因读数（provisional，待校准门控）**：**full 17 : through_trader_frozen 3 : tie 0**，
零解析失败，A 位先出示 8:12 位置均衡。frozen 胜出 3 对（300750/600030/601888）——评审
非无脑选边/选长。机器理由本批已落盘（1d46758 修复生效），三票明细随行。

**跨口径对照**：噪声版 19:1（§19.7）→ 冻结版 17:3——方向一致、幅度略收：上游采样噪声
确实贡献了约 2 对的偏移，但主结论**扛住冻结检验**：同字节上游下，风控辩论+FM 层使报告
在 17/20 对里被判更有助于决策；评审理由集中于「决策可执行性」（价位/止损/仓位 vs 泛泛
观望）。17:3 的 McNemar 式解读与 MDE（44pp）不在一个分辨率级——点估计 0.85 差异显著
超越门槛，仍待 4 对人工校准（`2026-09-19-p2-calibration-b5-frozen.csv` + 预填 + 报告直链
`2026-09-19-b5-frozen-calibration-reports/`）。


### 19.10 B5 外科手术对照臂：共享层全冻结下的纯层增量 full 20:0（2026-09-19，判定 60 次）

**由来**：owner 对冻结重跑版（§19.9）追问「都存在的章节不是应该共用吗」——再次命中设计
残缺：冻结重跑只冻结了分析师层，多空辩论/研究经理/Trader 仍各臂独立采样（残余混淆）。

**外科手术方案（零 LLM 构建）**：取 full 同一次 run 的 state，pop 风控/FM 族键后调
generate_report 重新渲染（决策段回退 trader_plan，report.py:361）——分析师/辩论/研究经理/
Trader/导语全部 byte 级共享，差异 = 风控辩论章节 + FM 章节的消失及决策段修正回退。
配套小改（TDD）：`generate_report` 渲染幂等——state 已有非空 focus_summary 时复用不重烧
（导语冻结用；首跑行为零变化）；`_build_focus_summary` 是 quick LLM 调用，此前「generate_report
零 LLM」的表述不实（frozen 重跑 llm_calls 实为 7/标的，更正）。
脚本 `p2_b5_surgical_arm.py`（第四关自检：共享层同一体验证 20/20 过，曾逮住 chart_data/
focus_summary 两个渲染键误入豁免名单外的疏漏）。

**纯层增量读数（provisional，待校准门控）**：**full 20 : full_no_riskfm 0 : tie 0**，
零解析失败，A 位先出示 9:11。机器理由（本批落盘）全为内容驱动：仓位降至 light + MA60
退出纪律 / 触发条件与置信度调整 / 最终审批与决策闭环——即风控层的决策打磨在每一对里
都被评审识别。

**三层读数对照（同题不同控）**：

| 对照臂 | 共享层 | 读数 | 解释 |
|---|---|---|---|
| through_trader（§19.7） | 无 | 19:1 | 上游采样噪声 + 层差异混杂 |
| through_trader_frozen（§19.9） | 分析师 | 17:3 | 残余下游采样噪声（3 对 frozen 胜 = trader 采样差，非辩论功劳） |
| full_no_riskfm（本节） | 全部 | **20:0** | 纯层增量：风控辩论+FM 的决策打磨无一对被评审判为负贡献 |

**决策段差异定性（owner 复核追问「都存在的章节为何不同」）**：共享层（二、三章）程序验证
逐字相同；决策段不同 = **层增量本体而非漏冻结**——full 版渲染 FM 裁决（final_trade_decision），
手术版按回退语义渲染 trader 原方案（report.py:361），两 state 的 trader_plan byte 相同，全部
差异（置信度 50%→55%、新增转多/转空触发条件与观察窗口、对激进派论点的逐条处理）皆由
风控辩论+FM 造成。若强行共用决策段则把被测层产出塞进对照臂，消融失效——「都存在」≠
「同源」，共享规则只适用于两臂独立运行的层。

**判定**：B5 终读 = **外科手术版 20:0（provisional）**；§19.7/§19.9 两版降级为方法学对照
（记录噪声如何随共享程度收敛：19:1 → 17:3 → 20:0）。校准 4 对
（`2026-09-19-p2-calibration-b5-surgical.csv` + 预填 + 报告直链）待人工；一致率 ≥0.80 后
B1/B2/B5 三腿全部转正。**方法学沉淀**：层增量归因一律用外科手术式对照（同 run 产物摘层
重渲染），独立采样臂只作稳健性旁证——已按 owner 原则写入 BACKLOG 纪律。


### 19.11 B5c 结论级盲评（owner 定稿判据）：风控终稿 19:1，价值集中在风险边界+可执行性（2026-09-19，66 次调用）

**口径**（预登记 §10，rubric b5c-v1）：只比两臂最终交易指令（同一 Trader 初稿的修改版 vs
未修改版，各 300–500 字），六准则（方向自洽/置信度校准/可执行性/风险边界/可证伪性/内部
无矛盾），硬排除「谨慎加分/决断加分/篇幅文风/流程提及」——直击 §19.10 的结构识别与
篇幅偏好病根。判据与提示词由 owner 逐条定稿。

**读数（provisional，待校准门控）**：真对照 20 对——**风控终稿(b) 19 : 初稿(a) 1 : tie 0**，
零解析失败；**暗对照（同臂对）2/2 判 tie**——位置偏差未检出，评审非无脑选边。

**决定性准则分布**（b 胜 57 票）：**风险边界 25 > 可执行性 22** > 可证伪性 6 > 内部无矛盾 2
> 方向自洽 1 > 置信度校准 1——风控层的价值高度集中在「把模糊观望变成带止损/触发条件/
仓位上限的可执行纪律」这一件事上。

**601899 初稿唯一胜出（内容级判断的试金石）**：终稿参数（止损29.4/目标34.5/入场31.68）
对应赔率 1.24:1，理由却自称「约 1.7:1」——数值矛盾被评审抓住判初稿胜。**评审不仅测量了
层价值，还抓到风控层自己引入的 bug**（处置候选：Trader/FM 赔率计算自检）。报告级 20:0 与
结论级 19:1 的差值恰在此对：内容更多 vs 结论更对，新口径分得开。

**四层读数对照**（同题四种控）：独立采样 19:1（混杂）→ 冻结分析师 17:3（残余噪声）→
报告级 20:0（内容量对照）→ **结论级 19:1（质量判据，终读）**。

**校准**：5 行（含 601899）`2026-09-19-p2-calibration-b5c.csv`——**一致率 5/5 = 1.00**
（000333 owner 逐条裁决 b + 4 行 owner 认可 AI 独立复核的批量核准；provenance 注：弱于
B1 逐行覆核，随采样协议 v2 下腿补抽查）→ **B5c 过门转正**。B5 终读定稿：**风控终稿 19:1**，
价值画像 = 风险边界+可执行性；P2 三腿（B1 0.875 / B2 0.906 / B5c 1.00）全部过门。

**AI 独立复核（2026-09-19，标注前自审）**：按六准则逐对独立裁决 20 对，**20/20 与机器
一致**（19b+1a）。关键复核发现：
- **接近对披露（3 对，换判不改变聚合）**：600276 / 600036 为最接近的 b 胜（初稿已有半具
  体触发条件，终稿增量是时间止损/量价确认，判 tie 亦属合理区间）；601899 为最接近的 a 胜
  （终稿结构更优——light 仓位/分批/止损缓冲——但赔率自述矛盾属承重错误，判 b 亦可辩）
- **无谨慎偏差的实证**：置信度变动双向（002352/300750 终稿反而 55→60 上调；601012 68→62；
  002594 55→48）——评审非机械选保守
- **601318 唯一方向变更（buy→watch）判据核验**：胜出理由是初稿参数不自洽（2.2% 止损 ≈
  单日 VaR，自称"严格止损"名不副实）而非"观望优于买入"——决断不加分护栏生效
- **赔率自述矛盾第二例**：000333 **初稿**自称"约2.6:1"而派生指标行是 2.00:1（机器未引用，
  不改判定）——与 601899 终稿（1.7:1 vs 1.24:1）合计两例、横跨 Trader 与风控层，赔率自检
  处置候选的证据 strengthened
- **002594 终稿改了价格参数**（止损 82.5→81.2 匹配波动率）——风控层不只会加纪律，也会
  重写执行参数


### 19.12 下轮全量材料跑批（新生产栈观测轮）：grounding 0/79、B5c 15:4:1 可执行性主导（2026-09-20，约 1000 次调用）

**动因**：§19.8 承诺「下轮全量材料跑批的 grounding 复扫照做」+ 三腿转正后的首轮常规观测。
生产栈相对 09-17 旧批的变化：辩手断言级锚点 prompt（已 deploy）、五件契约修复、#123 重算
注册表扩容、origin/main 整合收口（116da21）。**同一批 P1 固定快照**（20 标的，materials-20260920
新目录隔离，旧材料/缓存零污染）。

**执行**（全部新缓存文件，`PER_TICKER` 闸门口径与旧批对齐处见各腿）：
- 材料腿：full 变体 20/20（393 调用）；surgical a 臂 20/20（零 LLM，第四关共享层一致性全过）
- grounding 复扫（§19.8 承诺）：79 data 单元（bear r1）——**无源 0/79 = 0.000**，零解析失败；
  校准样本 8 行 `2026-09-20-p2-grounding-calibration.csv`（正例 0 → 全抽自有源）
- B1 吸收判定：**251 行全量**（闸门放开对齐旧批 196 全量口径；80+171 补判）——吸收率
  **0.833**（209/251，旧批 0.786 +4.7pp），per-ticker 0.40–1.00，零解析失败
- B2 修正判定：80 行（每标的 4 等距闸门）——修正率 **0.138**（11/80，旧批 0.198 −6pp），零解析失败
- B5c 结论级盲评（b5c-v1 判据不变）：**风控终稿(b) 15 : 初稿(a) 4 : tie 1**；暗对照 2/2 tie
  （位置偏差未检出）；校准样本 5 行 `2026-09-20-p2-calibration-b5c.csv`

**读数与归因**：
1. **grounding 0/79**：A/B 单节点 1.18% → 全量图 0%——新 prompt 在生产编排下成立（n=79 单尾
   95% 上界 ≈3.7%，残差量级与 A/B 一致）
2. **B5c 价值画像迁移**：决定性准则（b 胜 45 票）**可执行性 29 > 风险边界 6**——旧批是风险边界
   25 > 可执行性 22。方向不变（风控终稿优），但「把模糊观望变成可执行纪律」的具体形态从
   止损/仓位转向触发条件分层。跨批可比性注记：同判据同票制，材料为不同图运行产物
3. **赔率自述矛盾第三、四例**（处置候选证据再 strengthened）：600030（B 自称 1.78 vs 派生
   1.57）、601888（B 自称 1.55 且逻辑矛盾 vs 派生 2.23）——两对均因此判 a 胜。加上旧批
   601899 终稿、000333 初稿，共四例横跨 Trader/风控层/不同批次。**归因（本批实证）**：
   自检已挂 trader 初稿与 risk_judge 出口（`payout_ratio_corrected`），但本批两例矛盾文本
   均落在 **FM 终稿（final_trade_decision）——唯一未挂自检的决策出口**（601888 实测
   `payout_ratio_corrected: False`，初稿无矛盾）。处置候选精确化：FM 出口补挂
   `apply_payout_self_check`（实现已就绪，`fund_manager.py` 当前零调用）
4. **判定器自相矛盾行 1 条**：000001::b5c verdict=b 但票面理由明称「A更优」——judge 输出
   winner 与 reason 不一致（majority 仍判 b）；已入校准样本（owner 审表第一行即是），
   与 §19.3「新增维度封顶」同族的 LLM 输出一致性问题，暂不改判定器
5. B2 修正率 6pp 降幅：观测性漂移（同闸门 80 行），未过终裁不处置；与 B1 吸收率上升并读，
   方向与「决策层更接纳风险点」一致

**收口**：runs.jsonl `p2-batch-20260920`；metrics.md 时间线加行。B1/B2 校准不另出表（两腿已
转正，独立性由 §3 下腿抽查 47 行承载）；B5c/grounding 校准表各 5/8 行待 owner。


### 19.13 owner 终裁（2026-09-20 同日）：四表机器判定确认——B1 撤条件注、grounding 0/79 转正

**范围**：下腿抽查两表（B1 47 行 / B5c 6 行）+ 观测轮校准两表（grounding 8 行 / B5c 5 行，
含 000001 矛盾行）。owner 对话终裁「机器没问题」（表内人工列未逐行回填；B5c 校准
「4 行批量核准」同款轻量先例）。

**生效**：
- **B1 有条件转正 → 定稿**：§19.2 的补独立抽查条件（下腿 47 行含 35 判否正例层）经确认
  满足，条件注解除。「部分吸收」边界形态（601318 选择性引用 / 002027 到达未处理 /
  601899 确认未行动）经 owner 判读确认**维持严格二值口径**（部分不算吸收）——不加档、
  不立项拆项（低于处置规模阈值）
- **B5c 转正 provenance 补强完成**（分票 2 行在内的 6 行确认）
- **grounding 0/79 无源率读数转正**：判定器抽查（8 行有源无误判）+ §19.8 A/B 复核——
  新辩手 prompt 在生产编排下成立，§19.8 承诺全链闭环
- **000001 矛盾行**：判定 b 维持；理由-结论不一致记录为判定器输出一致性已知形态
  （§19.3 同族），不处置
- **唯一遗留处置项：FM 终稿赔率自检**（四例证据，§19.12 归因）——待 OpenSpec 立项
